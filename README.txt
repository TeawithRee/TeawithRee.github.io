# Tea with Ree image-path correction

This is a content-only path correction. The six image files already exist at the repository root. The affected HTML pages were requesting `/images/filename.webp`, but the repository has no `images/` folder.

Upload and replace only the nine HTML files in this package. Do not upload the whole site, do not upload images again, and do not replace `index.html`, `style.css`, `sitemap.xml` or `CNAME`.

After deployment, check all six image URLs and all 13 article pages on the live site.
