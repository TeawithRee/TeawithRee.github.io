# Tea with Ree site-only correction

## Upload these two files only

Upload and replace these files in the repository root:

- `index.html`
- `tea-notes.html`

Do not upload this instruction file, the ZIP itself or any other files.

## Delete these two confusing repository files

Delete these files separately in GitHub:

- `README.txt`
- `HOMEPAGE-preview-look.html`

Keep the clean `README.md`, `CNAME`, `BingSiteAuth.xml`, `.nojekyll`, `robots.txt`, `sitemap.xml`, `style.css`, all article HTML files and all image files.

## What this change does

The homepage Google title and descriptions now use the approved quiet editorial wording and no longer describe Books and Travel as current top-level sections. The homepage visible introduction is aligned with the same direction.

Tea Notes becomes a short, spacious landing page with one paragraph, one `Start a Conversation` email link, the three pathway names Reset, Renewal and Becoming and a short closing line. The dense application form, extensive programme details and future app material are removed from the landing page and can be developed separately later.

About is not changed by this package.

## After upload

Wait for GitHub Pages to finish deploying. Then tell Manus that the upload is live. The public homepage and Tea Notes page will be checked before any Google indexing request.
